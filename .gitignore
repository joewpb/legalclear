# Node
node_modules/
dist/
build/
.npm/
.eslintcache/

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# Environment
.env
.env.local
.env.*.local
.DS_Store

# IDE
.vscode/
.idea/

# Mobile
mobile/.expo/
mobile/node_modules/
mobile/dist/
.env.example

# Phase 23 generated packet artifacts (PDFs + ZIPs created at runtime
# by backend/src/services/packet_builder.py — never commit user output).
backend/storage/

# Scratch / upload artifacts — never commit
*.zip
dummy.pdf
LC-TEST-*.pdf
doc.txt
files*/
# Local diagnostic fixtures (manual test inputs, kept out of git)
LC-TEST-002.pdf
# Phase 10 form library — raw form data lives in Supabase storage + DB, not git.
# Keep the pipeline code + lightweight audit artifacts; ignore the 135MB payload.
forms/*.pdf
forms/*.txt
forms/*/
forms/circuits/
forms/text/
forms/crawled/
# Large harvest dumps (regenerable; out of history)
forms/index.json
forms/full_catalog.json
forms/form_fields.json
forms/circuit_forms_catalog.json
forms/circuit_scan.json
forms/downloads.json
# stray duplicate of forms/forms_manifest.json
/forms_manifest.json
# unrelated local scratch
screenshots/

# harvest enrichment artifacts (source of truth is Supabase)
forms/.enrich_checkpoint.json
forms/enrichment_output_harvest.jsonl
forms/enrichment_input_harvest.jsonl

# Whale agent local config
.whale/

# Supabase CLI artifacts
supabase/.temp/

# Generated schema diagram (large, regenerable)
supabase-schema-*.svg

# Opinion pipeline data exhaust — code only, never the 759 JSONs
scripts/opinion_pipeline/data/*
scripts/opinion_pipeline/logs/*
!scripts/opinion_pipeline/data/.gitkeep
!scripts/opinion_pipeline/logs/.gitkeep

# Claude Code local state — track skills/ and commands/ only
.claude/*
!.claude/skills/
!.claude/commands/

# dispatch run archives (local-only)
runs/_wt_archive/
